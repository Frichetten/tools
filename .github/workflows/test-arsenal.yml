name: test-arsenal

on: [push]

jobs:
  test-arsenal:
    runs-on: ubuntu-latest

    steps:
    - name: Run arsenal.sh
      run: |
        echo $(whoami)
        sudo su root
        echo $(whoami)
        sudo mkdir /home/nick
        sudo mkdir /home/nick/tools
        sudo mkdir /home/nick/scripts
        echo $(ls)
        sudo mv * /home/nick/tools
        cd /home/nick/tools
        echo "Y Y Y Y Y Y Y Y Y Y Y Y" | sudo ./arsenal.sh
      env:
        CI: true
